import java.util.Scanner;

public class Main {
    public static void main(String[] args) {

        Scanner scanner = new Scanner(System.in);
        double fir, sec;
        fir = scanner.nextDouble();
        sec = scanner.nextDouble();
        System.out.println(fir / sec);

        scanner.close();
    }
}
